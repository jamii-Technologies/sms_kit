build:
  image: ruby:$$RUBY_VERSION
  environment:
    - RAILS_ENV=test
  commands:
    - bundle install --quiet --path build/bundle
    - bundle exec rake test

cache:
  mount:
    - build/bundle
    - .git

matrix:
  RUBY_VERSION:
    - 2.2.0
    - 2.2.1
    - 2.2.2
    - 2.2.3
